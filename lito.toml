[workspace]
name = "lito"
members = ["frontend", "core", "cli", "test"]
default-members = ["cli", "test"]

[workspace.package]
version = "0.1.0"

[profile]
exceptions = false
rtti = false

[profile.perf]
inherits = "release"
opt-level = 2
debug = "line-tables-only"
strip = "none"
lto = "off"
