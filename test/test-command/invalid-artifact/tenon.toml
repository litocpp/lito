[package]
name = "fixture-invalid-test-artifact"
version = "0.1.0"

[executable]
name = "fixture-invalid-test-artifact"
discovery = "explicit"
sources = ["main.cpp"]

[test]
name = "fixture-invalid-test-artifact"
discovery = "explicit"
sources = ["main.cpp"]
