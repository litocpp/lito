[package]
name = "fixture-profile-linker-owned"
version = "0.1.0"

[executable]
name = "profile-linker-owned"
discovery = "explicit"
sources = ["main.cpp"]

[usage]
private-linker-options = ["-Wl,--strip-all"]
