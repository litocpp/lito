[package]
name = "cmake-provider-mix"
version = "0.1.0"
module = "cmake_provider_mix"

[library]
archive = "cmake_provider_mix"
discovery = "module"

[dependencies.fixture]
pkg-config = "tenon-fixture"
cmake = "TenonFixture"
target = "TenonFixture::fixture"
visibility = "private"
