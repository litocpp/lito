[package]
name = "cmake-installed-config-directory"
version = "0.1.0"
module = "cmake_installed_config_directory"

[library]
archive = "cmake_installed_config_directory"
discovery = "module"

[external-dependencies.cmake.fixture]
find-package = "TenonFixture"
config-directory = "lib/cmake/TenonFixture"
targets = [{ name = "TenonFixture::fixture", visibility = "private" }]
