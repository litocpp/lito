[package]
name = "cmake-adapter-install"
version = "0.1.0"
module = "cmake_adapter_install"

[library]
archive = "cmake_adapter_install"
discovery = "module"

[external-dependencies.cmake.fixture]
find-package = "Fixture"
path = "../../../cmake/package"
adapter = "adapter.cmake"
targets = [{ name = "Fixture::fixture", visibility = "private" }]
