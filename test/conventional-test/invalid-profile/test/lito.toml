[package]
name = "fixture-conventional-invalid-profile-test"
version = "0.1.0"

[test]
name = "fixture-conventional-invalid-profile-test"
discovery = "explicit"
sources = ["main.cpp"]

[profile]
exceptions = false
