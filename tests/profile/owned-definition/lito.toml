[package]
name = "fixture-profile-owned_definition"
version = "0.1.0"

[[bin]]
name = "profile-owned-definition"
discovery = "explicit"
sources = ["main.cpp"]

[usage]
private-definitions = ["NDEBUG"]
