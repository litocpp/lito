[package]
name = "fixture-dependency"
version = "0.1.0"

[lib]
name = "fixture-dependency"
module = "fixture.dependency"
archive = "fixture.dependency"
discovery = "explicit"
sources = ["source.cppm"]

[dependencies."fixture.base"]
path = "../base"
visibility = "public"
