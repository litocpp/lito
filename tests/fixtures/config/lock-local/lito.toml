[package]
name = "fixture-config-lock-local"
version = "0.1.0"

[lib]
name = "fixture-config-lock-local"
module = "fixture.config.lock.local"
archive = "fixture.config.lock.local"
sources = ["source.cppm"]
