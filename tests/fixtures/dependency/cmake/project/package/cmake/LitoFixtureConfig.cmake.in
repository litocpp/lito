@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/LitoFixtureTargets.cmake")

if(COMMAND lito_export_asset_set)
  lito_export_asset_set(
    NAME runtime
    ROOT "${PACKAGE_PREFIX_DIR}/share/lito-fixture/runtime"
    FILES runtime.bin)
  lito_export_asset_set(
    NAME runtime
    ROOT "${PACKAGE_PREFIX_DIR}/share/lito-fixture/resources"
    FILES nested/resource.dat)
endif()
