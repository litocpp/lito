[package]
name = "cmake-legacy-dependency"
version = "0.1.0"

[lib]
name = "cmake-legacy-dependency"
module = "cmake_legacy_dependency"
archive = "cmake_legacy_dependency"

[dependencies.fixture]
cmake = "LitoFixture"
target = "LitoFixture::fixture"
