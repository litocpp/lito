[package]
name = "fixture-associated-workspace-benchmark"
version = "0.1.0"

[[bench]]
name = "fixture-associated-workspace-benchmark"
discovery = "explicit"
sources = ["main.cpp"]

[dependencies.fixture-associated-workspace-library]
path = "../library"
visibility = "private"
