[package]
name = "fixture-scanner-module-kinds"
version = "0.1.0"

[lib]
name = "fixture-scanner-module-kinds"
module = "fixture.scanner.kinds"
archive = "fixture.scanner.kinds"
discovery = "explicit"
sources = [
  "src/lib.cppm",
  "src/iface.cppm",
  "src/internal.cppm",
  "src/impl.cpp",
]
