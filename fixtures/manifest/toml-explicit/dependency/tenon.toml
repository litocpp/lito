manifest-version = 1

[package]
name = "fixture.dependency"
version = "0.1.0"
module = "fixture.dependency"

[library]
archive = "fixture.dependency"
discovery = "explicit"
sources = ["source.cppm"]

[dependencies."fixture.base"]
path = "../base/tenon.toml"
visibility = "public"
