manifest-version = 1

[package]
name = "fixture.resolver.missing"
version = "0.1.0"
module = "fixture.resolver.missing"

[library]
archive = "fixture.resolver.missing"
discovery = "explicit"
sources = ["source.cppm"]

[dependencies.missing]
path = "../does-not-exist/tenon.toml"
visibility = "private"
